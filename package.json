{
  "name": "airlock-pdf-editor",
  "version": "1.0.0",
  "private": true,
  "description": "A browser-only PDF editor with a browser-enforced privacy guarantee: your PDF never leaves your device.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
